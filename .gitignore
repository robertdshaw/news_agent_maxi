# ── Python environment & secrets ───────────────────────────────────────────────
/venv/
/.env
.config/
/.emergency_save/
/.preprocess_EDA_fe/

# ── Repo metadata & notebooks ─────────────────────────────────────────────────
# Jupyter outputs
.EDA_3.ipynb

# ── Progress logs & intermediate data ─────────────────────────────────────────
/df_train_progress.parquet
/df_test_progress.parquet
/df_val_progress.parquet

# ── Source & generated data you can rebuild ──────────────────────────────────
/source_data/
/preprocessed_data/
/upworthy_data/
/model_output/
/images/

# ── Ignore all pickle files by default (will be re-included below) ───────────
*.pkl

# ── Now ignore everything under data/, then un-ignore processed_data ─────────
/data/**

# keep the folder itself
!/data/preprocessed/processed_data/
# keep all files & subfolders under processed_data
!/data/preprocessed/processed_data/**

# ── End of .gitignore ─────────────────────────────────────────────────────────
